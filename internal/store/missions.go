// Package store — data access for the missions table.
//
// Phase 2 will move the SQL currently inline in internal/api/missions.go into
// typed helpers here so the API layer only deals with models and contexts.
//
// TODO: implement in Phase 2 (see docs/migration-plan.md).
package store

// TODO: implement in Phase 2 — move the SQL currently inline in
// internal/api/missions.go into typed helpers here so the API layer
// only deals with models and contexts.
